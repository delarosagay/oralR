% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handle_ambiguous_notation.R
\name{handle_ambiguous_notation}
\alias{handle_ambiguous_notation}
\title{Handle ambiguous dental notation values}
\usage{
handle_ambiguous_notation(
  x,
  action = c("keep", "as_fdi", "as_universal", "remove", "na")
)
}
\arguments{
\item{x}{A character or numeric vector of tooth codes.}

\item{action}{Character string specifying how to handle ambiguous values:
\itemize{
\item \code{"keep"}: Returns the vector as is (default).
\item \code{"as_fdi"}: Adds an attribute marking ambiguous values as FDI.
\item \code{"as_universal"}: Adds an attribute marking ambiguous values as Universal.
\item \code{"remove"}: Returns the vector without the ambiguous entries.
\item \code{"na"}: Replaces ambiguous entries with \code{NA}.
}}
}
\value{
A modified version of \code{x}. For \code{"as_fdi"} and
\code{"as_universal"}, the vector includes an attribute \code{ambiguous_as}.
}
\description{
Identifies tooth codes that could belong to both FDI and Universal systems
(e.g., "11" through "32") and applies a specified action to resolve
the ambiguity.
}
\examples{
# Handle a vector of ambiguous tooth codes by forcing FDI notation
ambiguous_codes <- c("11", "12", "21")
handle_ambiguous_notation(ambiguous_codes, action = "as_fdi")
}
